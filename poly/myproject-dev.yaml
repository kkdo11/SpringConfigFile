server:
  port: 12100

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoints:
    health:
      enabled: true
      show-details: always
    beans:
      enabled: true
    info:
      enabled: true
    server:
      address: localhost
      port: 12100



poly: 'BigData Dev'